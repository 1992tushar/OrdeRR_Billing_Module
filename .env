# Database
DATABASE_URL=sqlite:///orderr.db
#DATABASE_URL = "postgresql://orderr_db_user:VORCj0qqoRQZO3H7fGwurX4EBLPr0ww1@dpg-d881pu1kh4rs73c969f0-a.singapore-postgres.render.com/orderr_db"

# ── Auth ────────────────────────────────────────────────────────────────
DASHBOARD_USERNAME=
DASHBOARD_PASSWORD=

# ── Invoice output ───────────────────────────────────────────────────────
# Absolute path to folder where generated PDF bills are saved
INVOICE_OUTPUT_DIR=./invoices

# ── OrdeRR integration (runtime HTTP — NOT a code import) ───────────────
# Billing's own base URL, used by OrdeRR webhook to forward dispatch photos
BILLING_BASE_URL=http://localhost:8001

# ── Anthropic (for OCR dispatch photo — Step 7) ─────────────────────────
ANTHROPIC_API_KEY=

# ── Staff WhatsApp numbers (comma-separated, for routing in Step 8) ─────
STAFF_PHONE_NUMBERS=9623882123

